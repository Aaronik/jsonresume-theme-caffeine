# Logs
logs
*.log
npm-debug.log*

# Runtime data
pids
*.pid
*.seed

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directory
# https://docs.npmjs.com/misc/faq#should-i-check-my-node-modules-folder-into-git
node_modules

# Bower dependency directory
bower_components

# Temporary local files
.tmp

# Public folder from build
# Removed because my mac (apple M3 chip) is having a lot of troubel building this, particularly
# around gulp-sass, which I'd prefer not to deal with. I believe it'll require installing
# dart-sass, which I'm sure is doablle, but I just don't want to do. So the idea is to build
# this on linux and use it on mac.
# public

# Keep your personal resume.json private and any generated pdf's
resume.json
resume_*.json
resume.pdf
